{
  "name": "yaml-multiline",
  "private": true,
  "description": "Find the right syntax for your YAML multiline strings",
  "dependencies": {
    "jstransformer-uglify-js": "^1.1.1",
    "pug-cli": "^1.0.0-alpha6"
  },
  "devDependencies": {},
  "scripts": {
    "watch": "pug -w index.pug",
    "deploy": "pug -O '{production:true}' index.pug && scp index.html favicon.ico favicon-512.png yaml-multiline.info:/var/www/yaml-multiline.info/"
  }
}
